import numpy as np
# a = [x**_ for x in range(100) for _ in range(3)]
# print(a)
nums = np.array([[2, 3], [1, 2], [2, 3]])
res=[1, 2, 3]
print(np.random.randint (0, len(nums), len(nums) ))
print()
